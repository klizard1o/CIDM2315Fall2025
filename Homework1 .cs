namespace Homework1;

class Program
{
    static void Main(string[] args)
    {
        int X = (int)2.5;
        int Y = (int)3.3;
        double Z = 4 * X * X + 3 * Y;
        Console.WriteLine("Value of Z = " + Z);

        // Equation is Z =4*X*X + 3Y, when X =2.5, Y =3.3
    }
}
